# HUB Pessoal — memória compartilhada entre máquinas (Claude Code)

> As sessões do Claude Code do **Mac (casa)** e do **PC da Empresa** NÃO compartilham histórico de chat.
> A única coisa compartilhada é **este repositório git**. Por isso este arquivo é a memória comum.
> **No início de cada sessão:** dê `git pull` e leia este arquivo. **No fim:** atualize o "Log de handoff" e dê `git add -A && git commit && git push`.

## Projeto
- App pessoal **single-file**: `index.html` (HTML/CSS/JS, minificado em linhas longas).
- Publicado no **GitHub Pages**: https://dkonrad88.github.io/hubpessoal/
- Repo: `github.com/dkonrad88/hubpessoal` — branch **`main`**.
- Áreas: Viagem Orlando (roteiro, checklists, gastos, documentos), Casamento (gastos, convidados, tarefas), Hábitos, Metas, etc.

## Onde vivem os dados (NÃO PERDER)
- **Código/layout** → `index.html` (versionado no git). Mudanças de layout vão pelo git/Codespace.
- **Dados do usuário** (checklists, convidados, gastos de viagem e casamento, hábitos…) → **Supabase** (sync na nuvem, dentro do app). **NÃO** ficam no git.
- ⚠️ **Regra de ouro:** nunca deixar o app subir um estado vazio/parcial por cima do Supabase. Antes de importar dados, garantir que o app já puxou a nuvem (senão um push sobrescreve a nuvem).

## Suíte de apps — o HUB é a central (CONVENÇÃO, ler sempre)
O dono tem vários apps pessoais e o **HUB é o painel central** deles.
- **Mesma origin + mesmo Supabase + mesmo login:** todos os apps ficam em `dkonrad88.github.io/<app>` (single-file, GitHub Pages), usam o MESMO projeto Supabase (`jlouesrrmqeauzlgvrpw`), mesma chave anon e mesma origin → a sessão de login é **compartilhada** (mesmo usuário em todos, sem novo cadastro).
- **Cada app grava na SUA tabela** no Supabase (ex.: `foco_state`, `et_progress`, `horas_state`), com RLS `auth.uid()=user_id`. O HUB só **LÊ** essas tabelas — dashboards **read-only** em módulos IIFE isolados (`var FOCO`/`ING`/`HORAS`). Liga por **leitura/link, NUNCA copia código** do app.
- **REGRA para todo app novo:** além do app em si, criar **uma aba dentro do HUB** com uma **dashboard das informações relevantes** (não só um botão "Abrir app" — o HUB precisa ter tudo). Sempre definir e documentar o **contrato** (tabela + formato do JSON) que o HUB vai ler.
- **Integrações atuais:** Foco → `foco_state`; Estudar Inglês → `et_progress`; Que horas sair? → `horas_state`.
- **Supercoffee:** app `dKonrad88/supercoffee` → `https://dkonrad88.github.io/supercoffee/`. Monitor read-only no HUB (`SUPERCOFFEE`) lê `sc_precos` e `sc_config`; coletor GitHub Actions grava via `service_role` a cada 4h e registra snapshots em `sc_historico`. Escopo rígido: somente Caffeine Army 380g unitário; exclui kits, combos, sachês, 140g e 220g. Tabelas de preços têm leitura pública porque os dados não são pessoais; escrita continua restrita ao coletor. O ML permanece sem dados até existir `MLB_ACCESS_TOKEN` oficial.
- **Lista compartilhada (`dkonrad88.github.io/lista`):** ✅ **app + backend prontos (2026-06-29)**; modelo membership (tabelas `listas`/`lista_membros`/`lista_itens`/`convites`, RLS de membership, Realtime). **Contrato em `lista/CONTRATO-HUB.md`.** ✅ **Módulo `LISTA` (dashboard read-only) já plugado no HUB** (commit 06e4f52): lê `listas`+`lista_itens`+`lista_membros`; mostra progresso X/Y, itens a comprar com autor, nº de membros. (Lista ainda com 0 itens no Supabase → mostra estado "vazia" até usarem o app.)
- **Apartamento (`dkonrad88.github.io/apartamento`):** ✅ **app + backend prontos (2026-08-10)**. Repo `dKonrad88/apartamento` (single-file, PWA), modelo **membership** (`listas` tipo=`apartamento` + `apartamento_state`, RLS `is_membro`). Contas fixas do mês, montagem do apê e divisão do casal. **Contrato + gotchas em `apartamento/CLAUDE.md`.** Módulo `APTO` (read-only) plugado no HUB. ⚠️ O `gastos` é **particionado por mês** (`gastos:YYYY-MM`, uma linha por mês) — num array único, dois lançamentos simultâneos se sobrescrevem.
- **Em construção (sessões próprias):** Strava (conector OAuth, secret em Edge Function).

## Fluxo entre as duas máquinas
O usuário avisa onde está; o Claude Code cuida do git:
- Usuário diz **"tô no PC da Empresa"** ou **"tô no Mac"**.
- **Ao começar:** `git pull` + ler este CLAUDE.md.
- **Ao terminar:** atualizar o "Log de handoff" abaixo, `git add -A`, `git commit -m "..."`, `git push`.
- **Mac (casa)** = máquina canônica de desenvolvimento (Codespace). **PC da Empresa** = alinhado e conectado, uso ocasional.
- `git pull` funciona nas duas (repo público). `git push` no PC precisa de login do GitHub configurado (token) — resolver na 1ª vez.

## Estado atual (2026-06-20, sessão do PC da Empresa)
- O PC foi **alinhado ao Mac** hoje: `index.html` do PC = `origin/main` (versão "grafite neutro", ~831 KB).
- Antes, o PC tinha uma versão paralela (~401 KB) com coisas que o Mac **não tem**:
  - **Roteiro detalhado Dias 1–5** (Vineland Premium Outlets, Keke's, Ford's Garage… timeline rica com `stops`).
  - Tema "Copa/Brasil" → **descartado** (o Mac seguiu pra "grafite neutro").
  - Correções de bug: restaurar **previsão** da lixeira (faltava em `LIX_TIPOS`); `htStreak` alinhado ao `htDone`; `cloudApply` chamando `ajustarCounters`; checklist `clAutoGrow` (textarea com altura 0 quando a aba está oculta); handler global de erro ignorando erros de extensão do navegador.
- Backup da versão paralela do PC: `index.BACKUP-pre-git-20260620-1020.html` (local no PC, **não** versionado).

## Pendências
1. ~~Trazer o roteiro detalhado Dias 1–5 para o Mac.~~ ✅ **FEITO em 2026-06-20** (no PC): os dias 2–5 detalhados foram mesclados no SEED do `const roteiro` em `index.html`. O `roteiroRepair` (chamado pelo `cloudApply`) re-injeta esses `stops` por cima do roteiro simples do Supabase — sem tocar no Supabase. Arquivo `roteiro-detalhado-dias-1-5.js` removido (já mesclado).
2. Reaplicar correções de bug que se perderam no alinhamento PC→Mac:
   - ~~Restaurar **previsão** da lixeira (`LIX_TIPOS`).~~ ✅ **FEITO em 2026-06-20 (Mac)** — chave `previsao` adicionada.
   - ~~`cloudApply` chamar `ajustarCounters`.~~ ✅ **FEITO em 2026-06-20 (Mac)** — `try{ajustarCounters();}catch(e){}` adicionado no fim do `cloudApply`.
   - ~~`htStreak` usar `htDone`.~~ ✅ **FEITO em 2026-06-20 (Mac)** — investigado: era bug real só p/ hábitos `metrica:'soma'` (log com valor 0 inflava a sequência). `htStreak` agora gateia em `htDone(h,log)`.
3. ~~**(ALTO) Robustez do sync com o Supabase.**~~ ✅ **FEITO em 2026-06-20 (Mac, cont. 3)** — concorrência otimista via `updated_at` + guarda anti-vazio + backup de conflito (nuvem ganha). Detalhe no handoff. Se um dia o sync sobrescrever algo, o estado anterior está em `localStorage['hub_orlando_cloudConflictBackup']`.

## Log de handoff (a sessão mais recente escreve no topo)
- **2026-08-10 — Mac (casa) — NOVO APP "Apartamento" (gastos do apê, Diego↔Débora) + integração no HUB:** App criado do zero no repo **`dKonrad88/apartamento`** (público, Pages no ar, `.nojekyll`, conferido com `curl` → **HTTP 200**). **Decisões de produto do Diego:** app separado + compartilhado com a Débora; **duas visões desde já** (mês corrente + montagem do apê, já que ainda vão se mudar — meta "Morar no nosso apartamento", nov/2026); **divisão por lançamento** (`pagou`/`divide`, default 50/50); **contas fixas automáticas**. **Backend:** tabela `apartamento_state (lista_id,key,value,updated_at)` PK(lista_id,key), RLS `is_membro` nas 4 operações, `anon` revogado; espaço "Nosso Apartamento" criado já com os **2 membros** (não precisa de código de convite); **`'apartamento'` adicionado ao trigger `tg_protege_lista`** (antes só casamento/viagem). **RLS testada de verdade** (transação com rollback): Diego 1, Débora 1, **não-membro 0 e INSERT bloqueado**; DELETE do espaço levanta "PROTEGIDO". **Contrato:** keys `config` / `recorrentes` / `montagem` / **`gastos:YYYY-MM` (1 linha por mês)** — a partição por mês é deliberada: num array único todo save reescreve tudo e dois lançamentos simultâneos se sobrescrevem (foi o que motivou a fila de gravação do Casamento em 02/08). **Contas fixas são "virtuais"**: o recorrente ativo aparece como conta prevista **sem gravar no banco** até o pagamento ser confirmado (evita mês fantasma) — o HUB reproduz essa regra, senão o total do mês fica errado. **No HUB:** `apartamento` entrou no `REG` do `EMBED` (`phone:true`) e no `DASHMOD`→`APTO`; `page-apartamento` deixou de ser placeholder "em breve" e virou `etd-head` + `#apartamentoDash`; módulo **`var APTO`** (após `CASA`) resolve o `lista_id` **por tipo** (não hardcoda) e lê só as 4 keys que usa (`.in('key',[...])`). 3 cards: mês (total/pago/a pagar/fixas + atrasadas + meta), montagem (investido vs orçamento) e divisão ("quem deve pra quem"). **Validação:** jsc parse OK nos dois repos; app testado no browser com shim (**3 bugs achados que o jsc não pega**: `#app` sem `display:flex` → tabbar fora da tela; `.t`/`.m` inline → texto sobreposto na Divisão; tag "atrasada" em item de montagem); fluxo real exercitado (conta prevista → confirmar pagamento → vira linha real, total sobe pela diferença, fila esvazia); mini-dash do HUB testada com stub e **conferida à mão** (total 4.254,70 / pago 3.049,80 / fixas 3.542,35 / acerto 862,55 — todos batem). Commit + push nos 2 repos.
- **2026-07-25 — Mac (casa) — HUB: moldura de celular em TODOS os "Trabalhar no app":** A pedido do Diego (print), todos os embeds usam o mesmo mockup de celular do Habit Tracker. Liguei `phone:true` no REG do EMBED também em ingles/corrida/paris/vorlando/casa (Foco/A Tempo/Lista/Supercoffee já tinham). Validado no browser: os 9 apps com `.app-embed-pane.phone`, jsc parse OK. Nota: a sessão do Supercoffee já tinha entrado (commit `ffbcbf4`, nav+page+módulo `SUPERCOFFEE` + entrada no meu EMBED REG) — fiz `git fetch` antes de editar/commitar (regra multi-máquina). Commit + push.
- **2026-07-12 — Mac (casa) — HUB v2 Bloco D.3: robustez — VIAG/CASA resolvem o lista_id dinamicamente (não hardcodam):** As memórias mostram que os espaços de casal **já foram deletados/recriados várias vezes** (IDs mudam entre sessões). Os módulos VIAG/CASA tinham o `lista_id` hardcoded → quebrariam na próxima recriação. Agora fazem **lookup por tipo**: `sb.from('listas').select('id,nome').eq('tipo','viagem'/'casamento')` (RLS `listas_select = is_membro(id)` → só retorna os espaços do próprio usuário) e então lê `viagem_state`/`casamento_state` do `lista_id` achado. **VIAG** escolhe a viagem cujo nome casa `/orlando/i` (há 2 viagens: Orlando + Paris), fallback 1ª; **CASA** pega a 1ª casamento. Novo estado `empty` ("nenhum espaço — crie/entre pelo app"). **IDs atuais hoje (só p/ referência, NÃO hardcodar):** casamento `74764948-…`, Orlando `ab3b1cf0-…`, Paris `7b95e89d-…`. Validado no browser com stub de 2 passos (listas→state): VIAG achou Orlando sobre Paris, CASA achou o espaço, números corretos, sem erro no console; jsc parse OK. Commit + push.
- **2026-07-12 — Mac (casa) — HUB v2 Bloco D.2: perf — tira renders mortos do boot:** Com Orlando/Casamento nativos fora do nav (Bloco C), os renders de boot dessas páginas órfãs eram trabalho jogado fora a cada abertura. **Removidos do boot:** `renderRoteiro` (reconstruía a timeline de 17 dias — o mais pesado), `renderCL`×4 (checklists), `renderGastos` (boot **e** o do `window.load`, que faziam Chart.js só p/ a Orlando), `renderDashboard`, `renderDocs`, + 2 linhas mortas que abriam `subsub-orlando`/`chev-orlando` (removidos do DOM no Bloco C). **Mantidos** os que alimentam páginas VIVAS: `renderNotes` (Notas global — id `notes-list`), `renderTags` (Etiquetas), `mvRender`/`lixRender` (sob demanda no setSimplePage). As funções removidas **seguem definidas** (setTab2/setTabCas as chamariam sob demanda se voltarem ao nav). **Confirmado seguro:** páginas gastosmes/apartamento/investimentos/presentes/contas são placeholders "em breve" (não dependem de render); alvos dos renders removidos (roteiro-list, filterCat, dash-cats, dArquivo) são exclusivos das páginas órfãs. **Validado no browser:** 9 módulos = object, todas as 16 páginas navegam sem falha, **zero erro no console**, jsc parse OK. Commit + push.
- **2026-07-12 — Mac (casa) — HUB v2 Bloco D.1: Corrida ganha card Garmin (prontidão & recuperação):** O módulo `RUN` agora lê **também `garmin_state`** (Promise.all com strava_state) e mostra um 3º card "❤️ Prontidão & recuperação": **HRV, sono, FC repouso, body battery** + **sparkline de HRV** (até 12 dias). Lógica de fallback: usa `daily` (hoje) se tiver valor, senão a **última leitura não-nula de `trends.days[]`** (hoje o daily/fitness vêm null, mas trends tem ~11 dias bons). Mostra "Garmin · conectado" + "sync há X" (de garmin_state.meta.last_sync). **Contrato garmin_state** (key/value por user_id): `daily`{date,hrv_avg,resting_hr,sleep_hours,body_battery_high,…}, `fitness`{vo2max,training_readiness,…}, `trends`{days:[{hrv,date,sleep_h,resting_hr,body_battery_high}]}, `meta`{connected,last_sync}. Read-only (coletor externo grava). Validado no browser com stub (Strava+Garmin reais): HRV 41ms via fallback, sono 7.6h, FC 55, BB 75, sparkline 6 dias, cards Strava intactos; jsc parse OK; screenshot conferido. **Varredura confirmou:** após o Bloco C, as páginas nativas page-orlando/page-casamento ficaram **órfãs** (0 refs aos nav removidos, `setTab2`/`setTabCas`/`toggleSubpage('orlando'/'casamento')` não são chamados em lugar nenhum) — sem erro, mas os renders de boot delas (renderRoteiro/renderCL×4/renderGastos/renderDashboard/renderDocs/renderNotes + o renderGastos do window.load) são **trabalho morto** a cada boot → alvo de perf no próximo passo. Commit + push.
- **2026-07-12 — Mac (casa) — HUB v2 Bloco C: RESOLVIDA a divergência Orlando/Casamento (HUB agora lê as tabelas compartilhadas):** As abas nativas velhas de Viagem Orlando (`setTab2`) e Casamento (`setTabCas`) liam o **hub_state velho** (cópia morta) — agora aposentadas do nav. **Nav repontado:** "Viagem Orlando" → `setSimplePage('vorlando')` e "Casamento" → `setSimplePage('casa')` (subitens simples; removidos os subsubgrupos roteiro/checklists/gastos/… e dashboard/convidados/…). **Páginas novas** `page-vorlando` e `page-casa` (padrão etd-head + seletor EMBED Resumo↔App). **2 módulos novos** (read-only, após PARIS): **`var VIAG`** lê `viagem_state` (lista `ab3b1cf0…`) → card com dias p/ viagem, gasto vs orçamento (converte US$→R$ via meta.cambio), dias de roteiro, % checklist (do checklist:<uid> do usuário), nº documentos; **`var CASA`** lê `casamento_state` (lista `74764948…`) → card orçamento (contratado=Σvalor tipo=fechado; projeção=contratado+Σvalorini planejados; barra dupla contratado/projeção vs meta) + card convidados (Σqtd pessoas, padrinhos, checklist) + tarefas abertas. **Helpers `_dash*`/`_brl`/`_clProg`/`_bar`** compartilhados. "Trabalhar no app" = iframe /viagem e /casamento (dados de verdade, escrita correta). **Divergência resolvida:** o HUB nunca mais mostra/edita o hub_state velho desses apps; o app vivo é a única superfície de escrita. Código nativo velho (page-orlando/page-casamento + funções clData/casData/setTab2/setTabCas) fica no arquivo mas **fora do nav** (sem breakage; candidato a remoção no Bloco de perf). **Validação:** matemática conferida contra verdade-base SQL (Orlando gasto R$66.382/80k=83%; Casamento contratado R$38.260, projeção R$253.260/270k=94%, 191 nomes) + teste no browser com dados sintéticos exercitando cada caminho (conversão US$, fechado/planejado, cancelado excluído, Σqtd, checklist walk, tarefas) → 100% correto; jsc parse OK; sem erro no console; screenshot conferido. **PENDENTE (task c do dono):** abas nativas de Roteiro/Gastos/Checklist no desktop lendo viagem_state (hoje o iframe do app vivo cobre isso). Commit + push.
- **2026-07-12 — Mac (casa) — HUB v2 Bloco B: página "Viagem Paris" (monitor de voo/hotel/câmbio):** Novo item de nav **Viagem Paris** (Finanças, antes do Apartamento, badge "monitorando") + **`page-paris`** (padrão etd-head, "Abrir app" → /viagem) + módulo **`var PARIS`** (IIFE read-only, após o boot do EMBED). Lê 3 tabelas (RLS SELECT p/ authenticated, qual=true — logado lê tudo): **`viagem_orcamento`** (1 linha id=1: alvo_total/alvo_pp, fx_eur_brl, hotel_nome/noite/noites/rating/estrelas/teto, **hotel_combos[]** = {voo,slug,total,titulo,hotel_noite,...}), **`viagem_monitor`** (1 linha/janela: slug, titulo, cia, pax, preco_total, preco_por_pessoa, **preco_nivel** low/typical/high, deep_link, checkin/checkout), **`viagem_historico`** (snapshots diários: dia, voo_brl, total_brl, hotel_eur, janela). Render: card "🏆 Melhor combo agora" (menor total, com Δ vs alvo em verde/vermelho) + grid de janelas (badge de nível, voo total/pp, hotel, total, "ver voo"→deep_link, melhor destacada verde) + card do hotel (€/noite×noites→€total→R$) + rodapé "atualizado há X". Junta janela(monitor)+combo(orcamento) por **slug**. CSS `.prs-*` injetado 1x (#prs-css). Registrado no EMBED (paris→/viagem) → seletor Resumo↔App. **CONTRATO (não quebrar sem revisar PARIS):** as 3 tabelas acima com essas colunas; slug casa monitor↔combo; preco_nivel ∈ {low,typical,high}. Coletor externo (GH Actions) popula; **HUB só LÊ**. **Dados reais hoje:** POA→Paris 3pax, abr(típico R$28.342)/mai(baixo R$28.977), Hôtel Léna €137, câmbio 5,85, alvo R$26.500 (combo abr R$1.842 acima). **Validado no browser** com stub dos dados reais (localhost sem login): render 100% correto, screenshot conferido, jsc parse OK. Commit + push.
- **2026-07-12 — Mac (casa) — HUB v2 Bloco A: embed "Trabalhar no app" (iframe do app vivo) nas páginas simples:** Novo módulo **`var EMBED`** (IIFE isolada, antes do `var RUN`) que injeta em cada página registrada um **seletor segmentado "Resumo ↔ Trabalhar no app"** + um pane de iframe, tudo via JS no boot (não edita a marcação de cada página). "Trabalhar no app" carrega o **app vivo num `<iframe>`** (`dkonrad88.github.io/<app>`) — mesma origin → **herda o login do Supabase**, sempre atualizado, grava na nuvem em tempo real, zero duplicação de código (regra da suíte). Registry `REG`: habits→/Foco, ingles→/english-trainer, horas→/horas, lista→/lista, corrida→/strava (metas fora: 404, é nativo no HUB via mvRender). `phone:true` usa **moldura de celular** (.app-embed-pane.phone, iframe 392px com bezel) p/ os mobile-first (Foco/Lista/A Tempo); Inglês/Corrida em largura cheia. **CSS novo** (após `.etd-openapp`): `.appseg`, `.app-embed-pane(.phone)`, `.app-embed-hint`, + 2 regras que escondem/mostram os panes por `.page[data-view]`. Iframe é **lazy** (src só no 1º clique em "App"). **⚠️ BUG achado e corrigido na verificação (browser, não o jsc):** eu tinha posto `_safe(EMBED.boot)` na sequência de boot (~L2916) que roda ANTES do EMBED ser definido (~L3080), e `_safe(EMBED.boot)` avalia `EMBED.boot` antes de chamar `_safe` → `TypeError` **abortava TODO o resto do script** (RUN/FOCO/LISTA/ING/HORAS/EMBED ficavam undefined). Fix: removida a linha; `EMBED.boot()` chamado (try/catch) logo após a definição do módulo. **Lição: jsc só pega sintaxe — verificar no browser pega runtime.** Validado no browser (localhost:8770, cópia em /tmp): 6 módulos = object, seletor+iframe corretos nas 5 páginas, alternância Resumo↔App ok, sem erro no console. Em localhost o iframe mostra login (origin ≠ github.io); em produção auto-loga. **Descobertas p/ os próximos blocos (DB real via MCP):** IDs reais das listas do casal — Casamento=`74764948-e020-47cc-b176-ed38af0884eb`, Viagem Orlando=`ab3b1cf0-0b0e-4378-b7f3-6cb374af02a0`, **Viagem Paris JÁ EXISTE**=`7b95e89d-4455-4823-9f62-232e363e13ff` (viagem tipo) com backend de monitoramento rodando (`viagem_monitor`/`viagem_orcamento`/`viagem_historico`: preço voo POA→Paris 3pax abr/mai 2027, ~R$18.7k, atualizado hoje). Run tem **strava_state**(254 ativ., ytd 72 corridas) **+ garmin_state**(daily/fitness/trends, connected). Tabelas de casal têm dim **lista_id** (viagem_state/casamento_state = lista_id,key,value). **PRÓXIMO (Bloco B/C):** página nova Viagem Paris (iframe /viagem + mini-dash do monitor); resolver divergência Orlando/Casamento (hoje as abas nativas leem hub_state velho — trocar p/ iframe do app vivo + mini-dash lendo viagem_state/casamento_state); enriquecer Run com Garmin. Commit + push.
- **2026-07-01 — PC da Empresa — App "Lista": features de eficiência + preços/total + realtime total (commit `043a937`):** A pedido do Diego ("melhore a planilha, eficiente e com features"). Escolheu (via pergunta): *usar mais rápido*, *preços e total*, *tempo real total*. Feito no repo `dKonrad88/lista` (Pages atualizado; cache `sw.js` `v2`→`v3`). **(1) Usar mais rápido:** **editar item** (lápis em cada linha → sheet com nome/categoria/qtd/preço/excluir; o lápis substituiu a lixeira, delete agora vive na edição); **autocomplete** via `<datalist>` alimentado por um **catálogo local por lista** (`localStorage['lista_app_cat_<listaId>']`, mapa nome→{n:contagem,c:categoria,p:preço}) que **auto-preenche categoria e preço** ao reconhecer o item digitado; **itens frequentes** = chips acima do campo (1 toque adiciona; somem quando o item já está pendente na lista). **(2) Preços e total:** **nova coluna `lista_itens.preco numeric` (nullable, aditiva)**; campo de preço no add e na edição (`parsePreco` aceita vírgula); card de progresso mostra **"No carrinho: R$X · previsto R$Y"**; preço aparece na meta da linha. **(3) Tempo real total:** **`listas` entrou na publication `supabase_realtime`** (+ `replica identity full`) → nome e categorias sincronizam ao vivo (`applyListaRealtime`), e trata exclusão remota da lista. **Contrato do HUB atualizado** (coluna `preco` + nota de realtime; **HUB não precisa mudar** — pode opcionalmente somar `preco` p/ mostrar total no futuro). Validado no PC: estrutura balanceada ({}=300, ()=897, []=39, script 3/3), UTF-8 mojibake 0. **Preview visual segue quebrado no PC** → Diego testa no iPhone ("Atualizar o app" no menu). Pendências oferecidas e não feitas: **desfazer** (excluir/limpar) e **compartilhar lista em texto** (o Diego não marcou nesta rodada). Nota: catálogo de sugestões é **por aparelho** (localStorage) — cada um monta o seu; a lista/itens/preços seguem compartilhados no Supabase.
- **2026-07-01 — PC da Empresa — App "Lista": correções de UX no mobile (testado no iPhone real):** O Diego testou o app publicado no iPhone e apontou bugs; corrigidos direto no repo `dKonrad88/lista` (commit **0c4c559**, Pages atualizado). ⚠️ Nota de contexto: a pasta local `G:\g_Diego\HTML\lista` tinha sido **removida** numa reorganização — **re-clonei do GitHub** pra editar (o app é a fonte da verdade no GitHub, não no disco). O repo já tinha evoluído numa sessão do Mac: **PWA instalável** (manifest+sw+ícones) e "Atualizar o app" no menu (commit `8dd1479`). **Correções desta sessão:** (1) **Bug do teclado no iOS** (a lista sumia ao focar "Adicionar item"): a `.addbar` deixou de ser `position:fixed` e virou item flex no rodapé; `.app` usa `height:100dvh` + `interactive-widget=resizes-content`; `.list-scroll` com `min-height:0`. (2) **Responsividade:** auth/onboard scrolláveis, título da lista com reticências. (3) **Tema escuro** com toggle no menu (paleta via variáveis + `body.dark`; persiste em `localStorage['lista_app_theme']`; respeita `prefers-color-scheme`; script inline no `<body>` evita flash). (4) **Excluir lista** (dono, `delete listas`) / **Sair da lista** (membro, `delete lista_membros`) no menu. (5) **Gerenciar categorias** (adicionar/excluir) no menu — compartilhadas via **nova coluna `listas.categorias jsonb`** (aditiva; RLS de update já é `is_membro`; contrato do HUB atualizado — **o HUB NÃO precisa ler `categorias`**, a categoria do item segue em `lista_itens.categoria`). `sw.js` bumpado `lista-v1`→`lista-v2`. Validado no PC por estrutura balanceada ({}=250, ()=678, []=33, script 3/3) + encoding UTF-8 (mojibake 0); **preview visual segue quebrado no PC** → o Diego valida no iPhone (via "Atualizar o app" no menu ou reabrir). Sub-pendência conhecida: se um membro muda categorias, o outro só vê ao reabrir (categorias não estão no Realtime — aceitável).
- **2026-06-29 — 🟢 HANDOFF CONSOLIDADO p/ CONTINUAR NO MAC — App "Lista de Mercado" compartilhada (Diego↔Débora):** Resumo único pra retomar de onde paramos. **STATUS: app NO AR e 100% pronto no backend; falta só o TESTE REAL do fluxo com a Débora e, opcional, o PWA.**
  - **⚠️ AÇÃO 1 — pegar o app no Mac:** o app "Lista" é um **repo SEPARADO `github.com/dKonrad88/lista`** (NÃO faz parte deste repo do HUB) — **no Mac ele ainda não existe localmente** → `git clone https://github.com/dKonrad88/lista.git` numa pasta irmã da do HUB pra editar. Publicado em **https://dkonrad88.github.io/lista/** (confirmado HTTP 200). Conteúdo do repo: `index.html` (single-file, ~30 KB, mobile-first teal), `CONTRATO-HUB.md`, `.gitignore` (exclui `.claude/`/`serve.ps1`/`launch.json`/backups). `gh` no PC está logado como `dKonrad88` (conta com K maiúsculo; URL minúscula funciona).
  - **⚠️ AÇÃO 2 — atualizar o HUB no Mac:** `git pull` neste repo pra pegar o **dashboard `LISTA`** já plugado (módulo IIFE `var LISTA`, aba **Rotina → "Lista de Mercado"** = `nav-lista`/`setSimplePage('lista')`/`page-lista`/`#listaDash`; lê `listas`+`lista_itens`+`membros_da_lista`, read-only, reusa CSS `.etd-*` + `.lst-*`) e este handoff.
  - **Backend Supabase (`jlouesrrmqeauzlgvrpw`) — TUDO FEITO e testado:** modelo **membership** (≠ `<app>_state` por user_id): tabelas `listas` / `lista_membros` / `lista_itens` / `convites`. RLS gateada por `is_membro(lista_id)` **SECURITY DEFINER** (evita recursão). Funções: `criar_lista(nome,tipo)`, `criar_convite(lista)→código 6 chars/7 dias`, `entrar_por_codigo(código)`, `membros_da_lista(lista)→(user_id,email,papel)`. **Realtime** em `lista_itens`+`lista_membros` (publication + replica identity full). Hardening: superfície `anon` fechada (execute só `authenticated`), `search_path` no trigger. **Teste de segurança real (transação c/ rollback): não-membro vê 0 itens; membro e convidado veem.** Nenhum `service_role` no HTML (só anon pública + RLS). **UUIDs:** Diego `7fec78a9-e0d4-4d7a-9a32-abdf16151593`; Débora `807fe468-627a-4e50-8969-3efec0cc4c7b` (`debiandrade8@gmail.com`, já tem conta).
  - **Contrato HUB↔app:** `lista/CONTRATO-HUB.md` (tabelas, colunas, RPCs, "como achar a lista do casal", invariantes que NÃO podem mudar sem quebrar a dash do HUB).
  - **O QUE FALTA (continuar daqui):** (a) **TESTE REAL no browser** (nunca foi aberto de verdade): Diego loga em dkonrad88.github.io/lista → **Criar lista** "Mercado" → gera **código** → Débora entra com o código → adicionar item → confirmar que aparece **ao vivo** nos dois e que o dashboard `LISTA` no HUB sai de "vazia". (b) **PWA opcional** (mesmo kit do HUB): `manifest.json` + `sw.js` (navegação network-first, **NUNCA** cachear Supabase) + ícones (apple-touch 180/192/512) — as meta tags `apple-mobile-web-app-*` já estão no `<head>` do app, falta o resto. (c) Melhorias possíveis: nomes/avatares dos membros, editar item, "limpar comprados", desfazer, mais tipos de lista.
  - **Limitações DESTE ambiente (PC) — no Mac não valem:** no PC não há node/python/php, a skill **phone-frame não está instalada**, e o **Claude_Preview trava** (webview irresponsivo) → validei só por estrutura balanceada + encoding UTF-8 (mojibake 0) + RLS no banco, **sem screenshot/preview visual**. **No Mac há `jsc` (validar JS) e o preview deve funcionar** → dá pra fazer o preview visual de verdade lá. (Memória local do PC registra isso em `preview-broken-on-pc.md`.)
- **2026-06-29 — PC da Empresa — App "Lista de Mercado" PUBLICADO no GitHub Pages:** Repo **`dKonrad88/lista`** criado (público) via `gh repo create` e push da branch `main` (commits `1991cae` app inicial + `e6ce2e3` .gitignore completo). **GitHub Pages ativado** via `gh api` (branch `main`, raiz `/`, HTTPS forçado). **Site confirmado no ar: https://dkonrad88.github.io/lista/ → HTTP 200** (build `built`, index.html servido com UTF-8/teal). `.gitignore` exclui `.claude/`, `serve.ps1`, `launch.json`, backups → repo só tem `index.html` + `CONTRATO-HUB.md` + `.gitignore`. Validado antes do commit: estrutura balanceada, encoding UTF-8 sem BOM, mojibake 0, **nenhum `service_role` no HTML** (só a chave anon pública, protegida por RLS). Nota: conta GitHub é `dKonrad88` (K maiúsculo) mas a URL `dkonrad88.github.io/lista` funciona (case-insensitive). **`gh` está autenticado no PC** (escopos repo/workflow). **PENDENTE:** Diego loga no app, cria a lista de mercado, gera código e a Débora (`debiandrade8@gmail.com`) entra → aí o dashboard `LISTA` no HUB sai de "vazia" e mostra os itens ao vivo.
- **2026-06-29 — PC da Empresa — Confirmado + empurrado o dashboard LISTA:** O Mac já tinha plugado o módulo `LISTA` no HUB (commit **06e4f52**), mas estava **local-ahead** (não tinha ido pro origin) e o CLAUDE.md ainda dizia "falta plugar". Conferi o módulo contra o **schema real do Supabase** (via MCP: tabelas `listas`/`lista_itens`/`lista_membros` com colunas `texto/qtd/categoria/comprado` — bate certinho), atualizei a linha da suíte (LISTA = ✅) e dei **push** (leva o dashboard pro GitHub/Pages). Nada a recodar — só faltava sincronizar.
- **2026-06-29 — PC da Empresa — NOVO APP "Lista compartilhada" (Diego↔Débora, Realtime) criado:** App novo em **repo/pasta separado** `G:\g_Diego\HTML\lista\` (single-file `index.html`, ~30 KB), pra publicar em `dkonrad88.github.io/lista`. **Modelo membership** (diferente do `<app>_state` por user_id): tabelas **`listas`** / **`lista_membros`** / **`lista_itens`** / **`convites`** criadas via Supabase MCP no mesmo projeto (`jlouesrrmqeauzlgvrpw`). **RLS de membership** gateada por `is_membro(lista_id)` `SECURITY DEFINER` (evita recursão de RLS). Funções: `criar_lista`, `criar_convite` (código curto 6 chars, 7 dias), `entrar_por_codigo`, `membros_da_lista` (retorna email dos co-membros, só p/ membro). **Realtime** em `lista_itens` + `lista_membros` (publication + replica identity full). **Segurança testada** (transação com rollback): Diego vê 2 itens, **estranho não-membro vê 0** ✅, Débora entra pelo código e passa a ver. Advisor: fechei superfície `anon` (revoke execute from public, só `authenticated`) e `search_path` no trigger. **App:** login Supabase (email/senha), criar/entrar lista por código, adicionar/marcar/remover item, categorias, contador X/Y + barra, agrupamento por categoria, sync ao vivo (otimista + reconciliação por `eventType`), convite via `navigator.share`/copiar. UI mobile-first teal. **Encoding limpo** (UTF-8 sem BOM, mojibake 0). **CONTRATO p/ o HUB em `lista/CONTRATO-HUB.md`** (tabelas, colunas, RPCs, como achar "a lista do casal", invariantes). ⚠️ **phone-frame skill NÃO existe neste ambiente** e o **preview ao vivo (Claude_Preview) está quebrado no PC** (sem node/python/php; webview irresponsivo) — validado por: estrutura balanceada, encoding, teste de RLS real no banco; **o app NÃO foi aberto num browser real ainda** (Diego vai testar o fluxo com a Débora `debiandrade8@gmail.com`, que já tem conta). Dev helpers locais: `lista/serve.ps1` (server estático PS) + `.claude/launch.json` no HUB (não precisam ir pro repo `lista`). **PENDENTE:** (1) Diego cria repo `lista` no GitHub + push + ativa Pages; (2) testar login+compartilhamento real com a Débora; (3) plugar módulo `LISTA` (dashboard read-only) no HUB seguindo o contrato. **Sem mudança no index.html do HUB** (só CLAUDE.md).
- **2026-06-28 — PC da Empresa — Convenção da suíte documentada + prompt da lista compartilhada:** Adicionada seção **"Suíte de apps — o HUB é a central"** no topo do CLAUDE.md (regra: todo app novo ganha tabela própria no Supabase + **aba/dashboard no HUB**, não só link; HUB lê read-only via IIFE; mesmo Supabase/origin/login). Entregue prompt para nova sessão criar **app de Lista de Supermercado compartilhada Diego↔Débora** (dado COMPARTILHADO entre 2 users via tabela com membros + RLS de membership + Supabase Realtime — diferente do padrão por user_id único). Também entregue antes o prompt do **conector Strava** (OAuth via Edge Function). Pendente: quando os contratos das tabelas (Strava, lista) saírem, plugar os módulos `STRAVA`/`LISTA` no HUB. Sem mudança em index.html nesta sessão (só CLAUDE.md + menu mobile já commitado em 150bc46).
- **2026-06-28 — PC da Empresa — Menu mobile (drawer overlay):** No mobile (≤760px) a sidebar vira um **drawer** que desliza da esquerda. Hambúrguer `.mnav-btn` (`ti-menu-2`) no header (só mobile) → `mnavToggle()` (classe `body.mnav-open`); backdrop `.sb-backdrop` (tap fecha via `mnavClose()`); listener delegado fecha o drawer ao clicar item de nav (onclick com setSimplePage/setTab2/setTabCas). No mobile `.sb-tools`/`.sb-reopen` escondidos e `.sidebar` vira `position:fixed;transform:translateX(-100%)`. **Desktop intacto.** Validado: delta de chaves 0 (baseline -3), mojibake 0. (Varredura geral de bugs/perf rodando em seguida.) Commit + push.
- **2026-06-28 — Mac (casa) — Integração "Que horas sair?" (app Horas) no HUB:** Mesmo padrão do Inglês. Menu **Rotina → "Que horas sair?"** (`nav-horas` → `setSimplePage('horas')`, ícone `ti-clock-hour-8`) + **`page-horas`** com dashboard read-only (módulo **`var HORAS`** antes do `var ING`, container `#horasDash`). Lê **`sb.from('horas_state').select('key,value').eq('user_id',cloudUser.id)`** (key/value igual foco_state), key **`'templates'`** = array de modelos. Renderiza por modelo: **hora de sair** (`fixedComp - anteMin - Σetapas` quando fixo), antecedência, nº de etapas, tempo de preparo (helpers `hhmm`/`fmtDur`/`tplTotal` portados do app). Reusa CSS de estado `.etd-msg/.etd-loading`; cards `.hrs-*` (grid 2col→1col <680px). Cabeçalho com **"Abrir app"** (`https://dkonrad88.github.io/horas/`) + atualizar. **Tabela `horas_state` criada via Supabase MCP** (idêntica a foco_state: user_id/key/value/updated_at, PK(user_id,key), RLS `auth.uid()=user_id`) → dash mostra "sem modelos" em vez de erro. **App Horas ainda é só localStorage** (`qhs_templates`) — vai ganhar Supabase numa sessão própria (prompt entregue). **Contrato fixo:** gravar templates em `horas_state` key `'templates'` no formato `qhs_templates` (`{id,name,etapas:[[label,h,m]],anteMin,fixedComp}`) — se mudar, a dash quebra. Ícones (relógio azul) já comitados no repo `horas`. Validado: jsc parse ok. Commit + push.
- **2026-06-28 — Mac (casa) — Integração "Estudar Inglês" no HUB (suíte de apps):** No menu **Conhecimento** entrou **"Estudar Inglês"** (`nav-ingles` → `setSimplePage('ingles')`, ícone `ti-language`) e a **página `page-ingles`**. Começou como card-launcher e evoluiu (a pedido) p/ um **dashboard read-only de evolução**, espelhando o padrão do `FOCO`: módulo **`var ING=(function(){...})()`** (inserido antes do `var FOCO`) que lê **`sb.from('et_progress').select('profile,data,updated_at').eq('user_id',cloudUser.id)`** — mesmo projeto Supabase, mesma origin, mesmo login. `et_progress` tem **1 linha por perfil** (`diego`/`debora`), coluna `data` = blob do english-trainer (`{level,streak{current,longest,last},srs{itemId→card},sessions[],tests[]}`). Renderiza 1 card por perfil com: nível, 🔥 streak (+recorde), revisar hoje (`dueCount`), palavras (total srs), consolidadas (`mastery`, intervalDays≥21) e barra **% consolidado** (mature/total). Funções `mastery`/`dueCount`/`isDue`/`todayISO` **portadas do `engine/srs.js`** do english-trainer → **se mudar o schema/SRS lá, revisar o módulo ING aqui** (mesmo acoplamento do FOCO↔foco_state). Estados nologin/loading/error/empty tratados. Cabeçalho da página tem **"Abrir app"** (link p/ `https://dkonrad88.github.io/english-trainer/`, app vivo — sempre atualizado) + botão atualizar (`ING.open()`). CSS `.etd-*` (grid 2col→1col <680px). **Read-only** (só o app de Inglês grava). Validado: jsc parse ok. **Decisão de arquitetura registrada:** HUB liga aos sub-apps por **link/leitura, nunca cópia de código** → cada app evolui na sua própria sessão e o HUB acompanha sozinho. Prompts de "kit PWA" entregues p/ as sessões do Foco e do Inglês (cada um vira instalável com ícone próprio). Commit + push.
- **2026-06-28 — Mac (casa) — PWA (instalar na tela do iPhone):** O HUB virou **PWA instalável** ("Adicionar à Tela de Início" no Safari → ícone próprio + tela cheia, sem barra do navegador). **Arquivos novos no repo:** `manifest.json` (name/short_name, `display:standalone`, `theme_color:#009C3B`, ícones), `sw.js` (service worker) e 3 ícones PNG gerados (`apple-touch-icon.png` 180, `icon-192.png`, `icon-512.png` — verde Brasil com "H" branco + faixa amarela, feitos via `qlmanage`+`sips` a partir de um SVG, já que não há ImageMagick/PIL no Mac). **No `index.html`:** meta-tags iOS no `<head>` (`theme-color`, `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style=default` (sem overlap na topbar), `apple-mobile-web-app-title=HUB`, `apple-touch-icon`, `icon`, `link rel=manifest`) + 1 `<script>` no fim registrando o SW (`navigator.serviceWorker.register('./sw.js')`, guard `'serviceWorker' in navigator`, em try/catch). **SW estratégia (importante):** navegação = **network-first** com fallback p/ cache (sempre pega o index.html mais novo quando online; abre offline se cair); **Supabase NUNCA passa pelo cache** (`/supabase\.(co|in)/` → deixa passar); demais GET (CDNs) = stale-while-revalidate; `CACHE='hub-v1'` (bump p/ limpar cache antigo). **NÃO** mexe no `roteiro`/seed (continua **enc16**) nem em dados. Validado: manifest = JSON ok; `sw.js` faz parse no jsc; servido local (`http.server`) com content-types certos; site no ar confirmado SEM PWA antes do push (404). ⚠️ **iOS não atualiza um ícone já adicionado** — pra ver o ícone verde + tela cheia, o usuário precisa **apagar o atalho antigo e re-adicionar** após o deploy do Pages. Caminho HTTPS (Pages) ok p/ service worker. Commit + push.
- **2026-06-27 — Mac (casa) — Feature: Modo Foco (uma parada por vez):** Nova visualização do roteiro que mostra **UM stop por vez** (modal tela cheia), pedida p/ usar durante a viagem ("estamos no almoço, mostra só isso"). **Abre em "Agora":** se hoje está na viagem (30/Set–16/Out, via `focoDiaDeHoje`), abre na parada do horário atual — `focoStopAtual` usa `focoParseHora` (regex `(\d{1,2})h(\d{0,2})`, tolera `~`) e pega o último stop com hora ≤ agora; fora das datas, abre na 1ª parada. Navega ←/→ (`focoNav`, atravessa dias nas pontas) + botão "Agora" volta ao momento. Cada tela: bolinhas de progresso, hora/ícone/título grandes, endereço→Maps (`focoMapsUrl` = `maps/search/?api=1&query=`), horário/fone, nota/alerta, seções de detalhes, "Depois: próxima parada + deslocamento". **Acessos:** botão **"▶️ Agora"** no `.page-actions` do Roteiro + botão **"Modo foco"** no topo do modal de cada dia (`renderDiaView`). Modal `#modalFoco` (padrão `.modal-overlay`+`openModal/closeModal`). **NÃO mexe em dados nem no seed** (continua `enc16`) — é só UI read-only (lê `roteiro[].stops`). Validado: jsc parse ok; lógica testada em JS real (`focoParseHora` + stop-atual: às 14h → parada do almoço ✓). ⚠️ NÃO verificado visualmente (preview não abre `~/Documents`) — usuário vai conferir no Pages. Commit + push.
- **2026-06-27 — PC da Empresa — Roteiro: info de retirada do carro no Dia 1 (enc16):** No stop `st1` (Chegada no aeroporto MCO), o `detalhes:[]` ganhou 2 seções: **"🚗 Retirada do carro (LEMBRAR)"** (condutor principal/titular só; CNH física impressa - não digital; cartão crédito internacional físico nominal p/ caução - não virtual; voucher impresso; caução pré-autorização USD; TAG pedágio pedir na retirada; FULL PROTECTION não cobre Road Service; taxa extra <25 anos; filmar/fotografar avarias antes) e **"🔄 Na devolução"** (tanque cheio→cheio + guardar comprovante/foto painel; pegar extrato e conferir estorno; não assinar sem conferir). `ROTEIRO_SEED_VER` enc15→**enc16** (reinjeta via `roteiroRepair`). **Validação reforçada (pós-bug do Fitbit):** saldo de chaves do arquivo = baseline -3 (delta 0); array roteiro balanceado; **mojibake real = 0** (Edit tool escreve UTF-8 ok, sem em-dash de propósito). Commit + push.
- **2026-06-22 — Mac (casa) — Reorganização dias 1–5 + refeições >4.5 + varredura de bugs (enc13→enc15):** **(1) Reordenação a pedido** (conteúdo move, datas ficam fixas na posição): Dia 1 mantém; **Dia 2 ← antigo Dia 3** (Clearance & Esporte); **Dia 3 ← antigo Dia 5** (Farmácia/Mercado); Dia 4 mantém; **Dia 5 ← antigo Dia 2** (Vineland). Objetivo: clearance (mais barato) cedo, Vineland (outlet de marcas) no fim. **Véspera do parque mudou de dia**: como o Magic Kingdom é Seg 05/Out, a véspera virou o Dia 5 (Vineland) — movido o aviso "amanhã o parque / preparar mochila" pra lá e removido do Dia 3. Datas/diaSemana/dataLonga reescritas por posição; `rota` e `gasto` movem junto. **(2) BUGS da reorg corrigidos** (varredura): Dia 1 dizia "Walmart no Dia 5"→Dia 3 (2 itens); Vineland dizia "faltar do Dia 3"→Dia 2. Mojibake = falso positivo (NÃO/ATENÇÃO maiúsculo). Datas/horários/ids/gastos/funções de mapa OK; duplicatas de lugares entre dias são intencionais. **(3) Refeições ajustadas à nova rota + nota >4.5**: Dia 2 almoço food court → **The Glass Knife** (276 S Orlando Ave, Winter Park, ~4.6 — mesma via do Fleet Feet/Nordstrom Rack); removido Whole Foods Winter Park do Dia 2 (mercado já no Dia 3). Dia 5 jantar **Ford's Garage (4.4)** → **Cooper's Hawk** (8005 International Dr, ~4.5, no caminho de volta). Mantidos Keke's (~4.7) e Ravenous Pig (~4.6). Cheesecake Factory (~4.3) mantido com nota transparente (dentro do Millenia, zero desvio). ⚠️ Notas vêm de reputação (WebSearch), NÃO da nota Google ao vivo — marcado "conferir no Maps" em cada parada. `ROTEIRO_SEED_VER` enc13→**enc15**. Validado: jsc parse ok; refs cruzadas limpas. Commit + push.
- **2026-06-22 — Mac (casa) — Rotas Google Maps por dia + reorganização do Dia 1:** **(1) Dia 1 reorganizado** (chegam de carro no aeroporto): **aeroporto MCO → Apple (Florida Mall) → Supermercado (Walmart Turkey Lake — abastecer a casa: comida/bebida/higiene/limpeza/farmácia) → apto**, SEM passar em casa antes (removida a parada "Chegada em casa" intermediária; Walmart reescrito com seções de mantimentos). **(2) Links Google Maps por dia:** cada dia ganhou campo **`rota`** (lista ordenada de endereços). Apto = **`6165 Carrier Dr, Orlando FL 32819`** nas pontas dos dias de Orlando; Dia 1 começa no MCO; Dia 13 apto→Miami; Dia 14 só Miami; Dia 15 Miami→apto; Dia 17 apto→outlet→MCO (car return). Função **`diaMapsUrl(r)`** monta `maps/dir/?api=1&origin&destination&waypoints` (origin=rota[0], dest=rota[-1], waypoints=meio, `%7C` como separador). **Botão "Abrir rota do dia no Google Maps"** no topo do modal do dia (`diaMapsBtn` em `renderDiaView`). **Card "Rotas rápidas"** no topo da aba Roteiro (`renderRotasRapidas`, chamado por `renderRoteiro`) — grid Dia 1..17, cada um com link direto. **`roteiroRepair` agora propaga `rota`** (`if(seed.rota)atual.rota=...`). `ROTEIRO_SEED_VER` enc10→**enc11**. `rota` montada a partir dos endereços geocodificáveis dos stops (curados) — dias com stops de endereço vago (2,4,5) levam só os pontos principais; dá p/ enriquecer com endereços exatos depois. Validado: jsc parse ok; `diaMapsUrl` testado em JS real (URLs corretas Dia 1/3/13/15). ⚠️ Endereço do apto fica visível no repo público (usuário escolheu essa opção, ciente). Preview MCP segue inutilizável aqui (não lê `~/Documents`; servers do launch.json quebrados). **Ajuste (enc11→enc12):** no Dia 1 o mercado virou **Publix at Lake Cay Commons** (9930 Universal Blvd — do lado do apto, I-Drive/Universal Blvd, 7h–22h) p/ compra rápida de chegada (cansados da viagem, sem o desvio do Walmart); o Walmart (grande, "com calma") ficou no Dia 5 com nota. Rota do Dia 1 atualizada (aeroporto→Apple→Publix→apto). **Ajuste (enc12→enc13):** incluídas as REFEIÇÕES fora de parque nas rotas — preenchidos endereços reais (que estavam vagos) de Keke's (7512 Dr Phillips Blvd), Ford's Garage (Vineland, 8201 Vineland Ave), The Ravenous Pig (565 W Fairbanks Ave, Winter Park), Whole Foods D5 (8003 Turkey Lake Rd) e Restaurant Row D10/D16 (W Sand Lake Rd); rotas regeneradas (13 refeições reais agora entram). Refeições DENTRO de parques/outlets e em casa seguem fora de propósito (a rota de carro é apto→parque→apto). Commit + push.
- **2026-06-22 — Mac (casa) — 🔴 FIX sintaxe (Fitbit) + roteiro de compras refeito (dicas Orlando Diferente):** **(1) BUG CRÍTICO corrigido:** o commit anterior do Fitbit (PC, `9a22800`) deixou `comprasExtrasInit()` malformada — `try{` externo SEM `catch`/`finally` e função não fechada (saldo de chaves +1). Erro de sintaxe fatal → o `<script>` inteiro NÃO carregava → app quebrado em produção desde aquele commit. Fix: `...catch(e){}}}` → `...catch(e){}}}catch(e){}}` (fronteira única antes de `function renderCL`). Validado com `jsc` (parse passa: erro vira `ReferenceError: window` em runtime, não mais `SyntaxError`). **Lição p/ o PC: SEMPRE rodar o validador jsc antes de commitar; salvar UTF-8.** **(2) Roteiro de compras refeito** com base em 22 vídeos do canal **Orlando Diferente (Fernanda)** — transcrições puxadas via `youtube-transcript-api` (pip; YouTube bloqueia IP após ~20 reqs seguidas). Aplicada a **regra de ouro dela: Clearance → Departamento → Outlet → Shopping**, ir cedo, garimpar o FUNDO, clearance sem troca. Lojas clearance adicionadas (nomes reais confirmados via WebSearch): **Orlando Outlet Marketplace** (CK/adidas/UA/Carter's Clearance · 5269 Intl Dr, ~200m do Premium Outlets) no **Dia 3**; **Dillard's Clearance** (3403 E Colonial, abre 12h/fecha seg) no **Dia 4**; **The Loop & Loop West** (Kissimmee — "Delupe": Nike/Converse/Tommy Clearance + Ross/TJ/Burlington/ULTA/Sephora) no **Dia 16**; **Nordstrom Rack Winter Park** (1090 Orlando Ave, colado ao Fleet Feet — ÚNICO lugar c/ On Running em desconto) no **Dia 3**; off-price Ross/Marshalls/Burlington ao lado da Vineland no **Dia 2**; Winter Garden Village como opção no **Dia 5**. Avisos de cupom (Simon VIP Club, RetailMeNot/Honey, cadastro e-mail das marcas) nos Dias 1-2. Editado via parse JSON do `const roteiro` (script Python idempotente); `ROTEIRO_SEED_VER` enc7→**enc8** (reinjeta por cima do Supabase via `roteiroRepair`). Validado: 17 dias, JSON ok, sintaxe ok. **Ajuste seguinte (mesma sessão, `enc8`→`enc9`):** ao reescrever o Dia 4 eu havia removido sem querer o item **Fitbit** da parada de tech (regressão do commit anterior) — restaurado; e o **Best Buy** virou parada flexível ("se sobrar tempo") p/ aliviar o Dia 4 (4 temas: Millenia + Dillard's Clearance + beleza + tech). Conferido no **preview** (jsc parse ok = só `ReferenceError: window` em runtime; app carrega sem `__errbox`; Dia 3 e Dia 4 renderizam certo). **Ajuste 2 (mesma sessão, `enc9`→`enc10`):** do 23º vídeo (Q7zL7hRZoBI) — **JD Sports** (Mall at Millenia, Suite 120, ao lado da GameStop) adicionada à parada do Millenia no **Dia 4**: tênis de LANÇAMENTO (Nike/Jordan/Samba/ASICS/On) a preço tabelado, não revenda — complementa o clearance. E nota opcional no **Dia 1** (parada Apple/Florida Mall): **PopMart/Labubu** (Suite 124) + Dillard's tradicional (bolsas de luxo secondhand). Nomes/endereços confirmados via WebSearch. ⚠️ **Nota sobre o preview MCP:** ele lê o `launch.json` do diretório PRIMÁRIO (`~/Documents/Hub Pessoal vCode`), não do repo, e não serve `~/Documents` — validar via `jsc` + `json.loads` é o caminho confiável aqui. Commit + push.
- **2026-06-22 — PC da Empresa — Adiciona Fitbit (Google) em comprar + roteiro + previsão:** Item "Fitbit (Google) - pulseira c/ monitor de sono" (texto ASCII de propósito, p/ não arriscar mojibake no Windows; dono não sabia o modelo exato — pode renomear). (1) **Seed `clData.comprar`** seção Tecnologia: novo `{id:'ci29',...}`. (2) **Migração `comprasExtrasInit()`** (espelha `lojasExtrasInit`, mas p/ `comprar`): flag `LS+'comprasExtrasV1'` + regex `/fitbit/i` + match por seção `/Tecnologia/i`; idempotente. **Chamada no `cloudApply` ANTES de `pvSyncFromComprar`** (ordem importa: adiciona ao comprar do Supabase → o `pvSyncFromComprar` cria o item de previsão sozinho, sem editar `previsao` na mão). (3) **Roteiro:** Fitbit na parada de tech (Dia 4, "Best Buy/eletrônicos"); `ROTEIRO_SEED_VER` enc6→**enc7** (reinjeta via `roteiroRepair`). Validado: array roteiro balanceado (17 dias), mojibake real=0, `enc7` ok. Commit + push.
- **2026-06-21 — Mac (casa) — Adiciona loja Pandora (visitar + roteiro + previsão):** (1) **Roteiro:** item "Pandora — pulseira e berloques (charms)" no "🛒 Comprar aqui" do Vineland Outlets (Dia 2) e Mall at Millenia (Dia 4); `ROTEIRO_SEED_VER` enc4→**enc5**. (2) **Checklist visitar:** Pandora (`id:'lj82'`) na seção v9 "Lojas — Tech & Acessórios" do seed `clData`. (3) **Migração `pandoraInit()`** (idempotente, flag `LS+'pandoraInitV1'` + checagem por regex /pandora/i): adiciona Pandora à `clData.visitar` (seção Acessórios) e um item à `previsao` (`{titulo:'Pandora — …',categoria:'Roupas & Acessórios',loja:'Pandora',moeda:'US$'…}`), depois `saveAll()`. **Chamada só no `cloudApply`** (após os repairs) — de propósito: roda DEPOIS que a nuvem é aplicada (senão `previsao=d.previsao` e `Object.assign(clData,d.clData)` apagariam o que foi adicionado antes do sync). Como `previsao`/`visitar` do usuário vivem no Supabase, editar só o seed não bastava. Testado em runtime (jsc). Sintaxe OK.
- **2026-06-21 — Mac (casa) — Roteiro: junta listas de compra duplicadas:** Vários stops tinham 2 seções de "o que comprar" (a "🛒 Comprar aqui" + uma 2ª redundante: "O que comprar — prioridades"/"Comprar"/"COMPRAR"/"Itens de tech"/"O que garimpar"). Editado via parse JSON do `const roteiro` (linha 1431): removidas **10** seções redundantes, preservados itens novos (Walmart D1: medicamentos+utensílios; Sawgrass D13: Saks Off 5th). Mantidas seções complementares (Estratégia, Marcas, Dicas, "Na hora da compra", Foco, COMER). `ROTEIRO_SEED_VER` enc3→**enc4** (reinjeta por cima do Supabase via `roteiroRepair`). **Regra de ordenação do roteiro (documentada p/ referência):** compras pesadas/de marca cedo (dias 2-5, antes dos parques: outlets premium → esporte/corrida → moda/beleza/tech → farmácia/suplementos+mercado), agrupadas por tema seguindo as listas "Comprar — X"; parques no miolo (6-11) com dias leves de recuperação; Miami como bloco (12-15, Sawgrass outlet no caminho); finais (16-17) p/ repor o que faltou + presentes + devoluções. Dentro do dia de outlet: chegar na abertura (10h), luxo primeiro. **01/Out = Dia 2 = Vineland Premium Outlets** (já é o dia de outlet/melhores preços). JSON revalidado, sintaxe OK.
- **2026-06-21 — Mac (casa) — Foco/HUB: gráficos + heatmap + sub-navbar por categoria:** Módulo `FOCO` ganhou visão de progresso. Toggle `Hoje · Semana · Mês` (`FOCO.setView`). **Semana/Mês** mostram: 3 cards de stat (check-ins, média/dia, maior sequência 🔥), **gráfico de barras Chart.js** (`#fdChart`, check-ins por semana/mês — últimos 12; usa o Chart.js já carregado no head, tema via `getComputedStyle` das vars `--muted/--border`, instância guardada em `chartObj` e destruída a cada render), **heatmap estilo GitHub** (17 semanas, CSS `.fd-heat`, intensidade = nº de check-ins/dia) e **ranking "dias ativos por hábito"** + chips de evitar (🛡️ sequência limpa). Novo **sub-navbar por categoria** (`.fd-catnav`, `FOCO.setCat(idx)` — índice em vez de nome p/ evitar aspas; "Todas" + cada categoria com bolinha de cor) que **filtra tudo** (cards, gráfico, heatmap, ranking) via `vis()`/`_byId`; só aparece se houver categoria nomeada. Métricas de atividade **excluem hábitos de evitar** (senão dias limpos inflariam). Tudo read-only, funções de domínio seguem portadas do Foco. Sintaxe validada (jsc, script inteiro 332k). Commit + push.
- **2026-06-21 — Mac (casa) — Foco no HUB: dashboard desktop nativo (substitui o iframe):** Trocado o iframe do Foco por um **dashboard desktop nativo** dentro do HUB. Novo módulo `var FOCO=(...)()` (IIFE isolada, ~213 linhas, inserido antes do `</script>` principal): **lê a tabela `foco_state` direto** (`sb.from('foco_state').select('key,value').eq('user_id',cloudUser.id)` — mesmo client/login do HUB) e renderiza em `#focoDash`. **Read-only** — o Foco continua o único que grava. As funções de domínio (`inPeriod/isDoneLog/metaProgress/metaLine/cleanStreak/streakSoft/expectedToday/cadence/catOrder…`) são **portadas verbatim do Foco** (números batem 100%). Layout: header com data + "Atualizado HH:MM" + botões Atualizar/Abrir Foco; resumo "X/Y concluídos hoje" com barra; cards agrupados por categoria (ícone, status de hoje, meta+barra, sequência 🔥/🛡️, mini-tira de 7 dias). CSS `.fd-*` injetado 1x (`#fd-css`), usa as vars de tema do HUB (claro/escuro). `setSimplePage('habits')` agora chama `FOCO.open()` (pull+render a cada abertura → sempre atualizado) em vez de `htRenderAll()`. Sintaxe validada (jsc, script inteiro). Tracker antigo `ht*` segue morto/inofensivo no arquivo; dados velhos seguem no `hub_state`. **Próximo possível:** permitir marcar feito pelo HUB (escreveria em `foco_state` — exige replicar o formato de log do Foco; ficou de fora de propósito). Commit + push.
- **2026-06-21 — Mac (casa) — Integração Foco no HUB (embed) + welcome:** A aba **Habit Tracker** do HUB agora **embute o app Foco** (`https://dkonrad88.github.io/Foco/`) num `<iframe id="focoFrame">`, **substituindo** o tracker antigo (dados desatualizados). **Revertendo a decisão antiga "NÃO integrar"** — o motivo dela (Supabases diferentes) estava errado: os dois usam o **mesmo projeto** (`jlouesrrmqeauzlgvrpw`), **mesma chave anon** e o **mesmo origin** (`dkonrad88.github.io`) → o iframe herda login/sessão automaticamente, sem novo cadastro. Foco segue **fonte única** da verdade (grava em `foco_state`; HUB em `hub_state` — não se atropelam). **Sem fusão de dados de propósito:** schemas de hábito/log divergem (Foco: `cat/tipo/periodo/alvo/unidade/recaida…` · HUB: `categoria/meta/campos/valores…`) → traduzir seria frágil. A UI antiga do tracker foi removida da página (antigas linhas 1015–1030); o JS `ht*` e o CSS continuam no arquivo, inofensivos (`htRenderAll` no boot e em `setSimplePage('habits')` já estão em try/catch). Dados antigos seguem no `hub_state` (recuperáveis). Também: welcome screen com mensagem mais animada (emojis + áreas). Commit + push.
- **2026-06-21 — Mac (casa) — Assistente IA Fase 3 (mais tools + confirmação):** Edge Function `claude-chat` v3 com 4 ferramentas: `adicionar_item_comprar`, `adicionar_previsao`, `lancar_gasto` (gasto Orlando: `{id,data,desc,cat,subcat,moeda,valor,pagou:['Diego'],divide:['Diego','Débora']}`, cat default 'Outros'), `adicionar_tarefa_casamento` (`{id:casCounter++,done,titulo,quem,data}`). No HUB: agora **confirmação antes de aplicar** — quando a Claude pede tool_use, `aiAddConfirm` mostra um card "O assistente quer fazer: …" com **Aplicar/Cancelar** (`aiConfirm`/`_aiResume`); só executa no Aplicar (Cancelar manda tool_result "cancelado"). `aiToolLabel` formata cada ação. Continua só-adiciona, client-side, visível, salvo+sincronizado. Precisa do secret `ANTHROPIC_API_KEY`. Commit + push.
- **2026-06-21 — Mac (casa) — Assistente IA Fase 2 (tool use):** O chat agora PODE adicionar itens. Edge Function `claude-chat` v2 declara 2 tools (`adicionar_item_comprar`, `adicionar_previsao`) e devolve `{content, stop_reason}` (não mais só `text`). No HUB: `aiSend` virou loop de tool use (`stepLoop`, máx 5 voltas) — manda mensagens, recebe `tool_use`, executa via `aiExecTool` (adiciona em `clData.comprar`→`pvSyncFromComprar` ou em `previsao`), mostra linha 🔧, manda `tool_result` de volta pra Claude fechar com texto. **Só adiciona (nunca apaga), client-side, visível, salvo+sincronizado.** Continua precisando do secret `ANTHROPIC_API_KEY` no Supabase. Fase 3 possível: mais ferramentas (gastos, tarefas casamento) + confirmação antes de aplicar. Commit + push.
- **2026-06-21 — Mac (casa) — Assistente IA (Claude) no HUB, Fase 1:** Chat dentro do app. Arquitetura segura: Edge Function `claude-chat` no Supabase (projeto Habit Tracker `jlouesrrmqeauzlgvrpw`) atua como **proxy** — guarda a chave da Anthropic como **secret `ANTHROPIC_API_KEY`** (Deno.env), nunca no HTML (Pages é público). `verify_jwt` ligado → só quem está logado chama. Modelo `claude-haiku-4-5`, max_tokens 1024. No HUB: botão ✨ no header → painel `#panel-chat` (reusa `.global-panel`), funções `aiSend/aiAdd/aiSystem` (manda `messages` + system com contexto leve via `SB_URL+/functions/v1/claude-chat` com Bearer do session token). Histórico limitado aos últimos 12. **PENDENTE (ação do dono):** criar chave em console.anthropic.com (com billing) e setar como secret `ANTHROPIC_API_KEY` no painel do Supabase (Edge Functions → Secrets) — sem isso o chat responde "ANTHROPIC_API_KEY não configurada". **Fase 2 (futuro):** tool use pra o chat alterar checklist/previsão. Commit + push.
- **2026-06-20 — Mac (casa) (cont. 5):** UX dos modais do roteiro: botão de fechar virou **X sticky no topo** (novo `.modal-head` + `.modal-x`, header gruda no topo do `.modal` que rola). `modalDiaView` perdeu o botão "Fechar" de baixo (sobraram Editar dia / Adicionar parada); `modalStop` e `modalDia` ganharam o X no topo e mantiveram Cancelar/Salvar. Commit + push.
- **2026-06-21 — Mac (casa) — GRANDE LEVA (roteiro + features):** (1) **Roteiro Orlando** detalhado dias 1-17 (parques c/ rope drop/Lightning Lane, Epic Universe 2 dias, Universal sábado, Miami 2 dias, dias leves, embarque voo 19h25). Cada parada de loja tem seção **"🛒 Comprar aqui"** com itens da lista `comprar` do usuário; integra os lugares reais da lista `visitar` (Be Our Guest, Ravenous Pig, Wynwood, Design District, Bayside, Fleet Feet, Rack Room, Publix, TJ Maxx, Burlington, Ross). `ROTEIRO_SEED_VER` agora **enc3**; `roteiroRepair` re-injeta tb `data`/`gasto`. (2) **Viagem — Simulação de Gastos** na aba Previsão (`renderSimViagem`): orçamento×real(cota-casal)×previsto, derivado, zero persistência nova. (3) **Previsão — coluna "Loja sugerida"** + `pvSugestaoInit` (import idempotente da lista Comprar agrupado, com backup em `pvPreSugBackup`, flag `pvSugDone`, dedup por título, respeita exclusões). (4) **Casamento**: countdown + barra Pago-vs-Fechado (`casData` nos 4 pontos de sync) e **projeção corrigida** = fechados(valor final)+planejados(valor **inicial**). (5) **Foco Hábito**: decisão = **NÃO integrar dados** (Supabases/modelos diferentes, risco de acoplar, regra de não mexer em outros apps); no máximo um deep-link futuro (precisa da URL do Foco). MVPs dos grupos vazios (Presentes&Datas, Contas&Assinaturas) **NÃO** foram feitos ainda (design pronto em /tmp do agente; ficou pra próxima). Tudo validado (sintaxe JSC, sync, sem mojibake) e no ar.
- **2026-06-20 — Mac (casa) (cont. 10):** Backlog UX #3 (CSS, aditivo) + limpeza: `*:focus-visible{outline 2px #009C3B !important}` (foco de teclado visível — havia 26 `outline:none` matando o foco; usei !important pra vencer), `.btn:active{translateY(1px)}` (feedback de toque), `@media(prefers-reduced-motion)`. **NÃO** mexi no `--muted` do tema claro (dono pediu pra não alterar o tema claro — fica como opcional). Código morto removido: função `toggleDia` (substituída pelo modal central do roteiro, nunca mais chamada). Varredura de performance: **sem problema real** pra app single-user (14 `.page` no DOM e `exportPage` de 840KB não justificam refatorar — contra "não complicar"). Commit + push.
- **2026-06-20 — Mac (casa) (cont. 9):** Backlog UX #2 — **rede de proteção destrutiva**: `confirm()` adicionado em `lixExcluir` (aviso forte "apagar definitivamente, não pode ser desfeito" — é irreversível), `deleteNote` e `delConv` (aviso leve "Excluir X?" — vão pra lixeira, recuperáveis). Espelha o padrão que `deletePrevisao`/`lixEsvaziar` já tinham. Fecha a inconsistência (alguns apagavam no 1º clique, outros confirmavam). Commit + push.
- **2026-06-20 — Mac (casa) (cont. 8):** Backlog UX #1 — **pacote de confiabilidade do sync** (3 itens): (1) `visibilitychange`: ao esconder a aba → `saveAllFlush()` (flush à prova de mobile, iOS/Android ignoram beforeunload); ao voltar (após >8s, guard via `cloudLastSync`) → `cloudSyncBoot()` re-checa a nuvem e puxa se mais nova (resolve "abrir laptop com aba velha e sobrescrever o celular"); (2) `pagehide` → `saveAllFlush()`; (3) `importJSON` agora pede `confirm()` (avisa que SUBSTITUI tudo) e salva backup do estado atual em `localStorage['hub_orlando_preImportBackup']` antes de aplicar. Tudo aditivo, em try/catch, sintaxe validada. Próximos do backlog: rede de proteção destrutiva (confirm em lixExcluir/deleteNote/delConv), leva de CSS (foco visível, :active, contraste --muted), pacote mobile (sidebar-overlay + hambúrguer). Commit + push.
- **2026-06-20 — Mac (casa) (cont. 7):** Mojibake do roteiro VOLTOU mesmo com o seed do arquivo já limpo — causa: o dado corrompido já estava no **localStorage + Supabase** (empurrado antes), e o `roteiroRepair` só injetava o seed quando o dia estava SEM paradas → pulava. Corrigido com **versão de seed**: `var ROTEIRO_SEED_VER='enc1'` + carimbo `atual.seedVer`; re-injeta o seed limpo uma vez quando `seedVer` não bate (boot e cloudApply), `saveAll()` se mudou, e não atropela edições futuras (após carimbar). Display dos Dias 1–5 sempre limpo em qualquer máquina, independente da nuvem. ⚠️ O blob no Supabase ainda tem o mojibake ARMAZENADO (nunca exibido); pra limpar na fonte dá pra fazer via MCP com backup. Pra re-forçar no futuro, bumpar `ROTEIRO_SEED_VER`. Commit + push.
- **2026-06-20 — Mac (casa) (cont. 6):** Filtro dos **Gastos do Casamento**: o `<select>` "Planejando" virou **pills horizontais** (sub-navbar) reusando o estilo `.cl-tab`/`.cl-tab.active` que já existia. 3 pills (Fechados/Planejando/Todos) com `data-f`; `setCasGastoFiltro` passou a alternar a classe `active`. Sem CSS novo, lógica de filtro intacta. Commit + push.
- **2026-06-20 — Mac (casa) (cont. 4):** Corrigido **mojibake** (UTF-8 duplo-codificado) no seed do roteiro: ícones/emojis e acentos apareciam como "CafÃ©", "Â·", "ðŸ›", "â€"". Origem: commit 7441db4 (mescla dos Dias 2–5 feita no PC/Windows com encoding errado) — não havia versão limpa no git. Reparado com `ftfy.fix_encoding` (Python) aplicado ao arquivo: 149 "Ã" → 4 (os 4 são texto correto: NÃO/PREVISÃO/PADRÃO). Texto já correto preservado, estrutura HTML/JS intacta, sintaxe validada. **CUIDADO no PC:** ao editar o index.html no Windows, garantir que o editor salve em UTF-8 (não cp1252/latin1) pra não reintroduzir mojibake. Commit + push.
- **2026-06-20 — Mac (casa) (cont. 3):** Robustez do **sync com Supabase** (achado ALTO da varredura). Concorrência otimista usando a coluna `updated_at` (já existia — sem mudança no Supabase): (1) novo `cloudSyncTs/cloudSetSyncTs` guarda no localStorage qual versão da nuvem já temos; (2) `cloudStateEmpty` + guarda no `cloudPush` → nunca sobe estado vazio; (3) novo `cloudSyncBoot` no boot compara `updated_at` remoto vs local → se a nuvem é mais nova, **puxa** (mesmo dirty); senão, se dirty, empurra; senão ok. Substitui o antigo `if(isDirty())cloudPush();else cloudPull()`; (4) `cloudPull` agora seleciona `updated_at`, grava o syncTs e, **se for puxar com mudanças locais não sincronizadas, salva backup recuperável** em `localStorage['hub_orlando_cloudConflictBackup']` antes de sobrescrever (política: **nuvem ganha**, local vira backup). Sintaxe validada via JavaScriptCore. `beforeunload`/flush do push deixado de fora de propósito (push async não garante completar na saída). Commit + push.
- **2026-06-20 — Mac (casa) (cont. 2):** Varredura de bugs no `index.html`. Portei a correção **#2** (`htStreak`): agora busca o hábito e só conta o dia na sequência quando `htDone(h,log)` é true (gateia a meta `'soma'` — antes qualquer log, mesmo valor 0, inflava o streak; booleanos não mudam). Guarda `if(!h)return 0` evita erro com log órfão. **Achado ALTO ainda em aberto (não mexido):** sync é last-write-wins sem trava — no boot `cloudInit` faz `if(isDirty())cloudPush()` (empurra sem puxar/comparar), `cloudPush` não compara `updated_at` nem barra estado vazio, e `beforeunload` só faz `saveAllFlush` (não faz flush do push debounced 1800ms). Risco de uma máquina sobrescrever dados mais novos da nuvem. Decidir desenho antes de mexer (pull-first + comparar `updated_at` / guarda anti-vazio). Commit + push.
- **2026-06-20 — Mac (casa) (cont.):** Portei a correção **#3**: `cloudApply` agora chama `try{ajustarCounters();}catch(e){}` no fim (junto dos repairs), após aplicar os dados do Supabase. Antes só o `loadAll` chamava — então, após um sync da nuvem com IDs de Casamento mais altos que o `casCounter`, o próximo item de Casamento podia colidir/sobrescrever. `ajustarCounters` só eleva `casCounter` (max id +1), idempotente, não toca em dados. Resta só a #2 (`htStreak`/`htDone`, não portar às cegas). Commit + push.
- **2026-06-20 — Mac (casa):** Alinhei o Mac ao `origin/main` (fast-forward, 12 commits do PC). Antes disso, nesta sessão do Mac: tema escuro migrado de azul-marinho para **grafite neutro/quente** (só o bloco `body.dark`; header Copa/bandeira mantido). Depois do pull, portei a correção **#1 da pendência**: a lixeira não tinha a chave `previsao` em `LIX_TIPOS` → o restore já funcionava (o `case 'previsao'` existe no `lixRestaurar`), mas o rótulo aparecia como "previsao" cru e a tela não atualizava ao restaurar. Adicionei `previsao:{nome:'Previsão',render:renderPrevisao}`. **Ainda pendentes (não portadas):** #2 `htStreak` usar `htDone` (NÃO portar às cegas — o `htStreak` atual calcula via `htLogs`; investigar o bug real antes) e #3 `cloudApply` chamar `ajustarCounters` (confirmado ausente; baixo risco, candidato a portar). Commit + push.
- **2026-06-20 — PC da Empresa (cont. 10):** Checklist: seções começam **todas recolhidas** a cada load. Nova `clCollapseAll()` (fecha todas as seções de `clData`) chamada no boot após `loadAll` e no fim do `cloudApply`. Toggle segue normal na sessão. Commit + push.
- **2026-06-20 — PC da Empresa (cont. 9):** Reapliquei a correção do **checklist em branco** (itens só apareciam após recolher/expandir): `clAutoGrow` não zera altura quando oculto + `setCLTab`/`setTab2` re-renderizam o painel ao ficar visível. ⚠️ Essa correção (e a do filtro de erro de extensão) tinham se PERDIDO no alinhamento PC→Mac. **Possíveis outras correções do PC ainda NÃO portadas pro Mac** (reaplicar se aparecerem): restaurar previsão da lixeira (`LIX_TIPOS`), `htStreak` usar `htDone`, `cloudApply` chamar `ajustarCounters`. Commit + push.
- **2026-06-20 — PC da Empresa (cont. 8):** Roteiro: clicar no dia agora abre um **modal central** (`#modalDiaView`) com a timeline pra ver/editar, em vez de expandir inline. Novos `openDiaView()`/`renderDiaView()`/`diaViewIdx`; reusa `roteiroStop`; footer com "Editar dia" e "Adicionar parada" (abrem `modalDia`/`modalStop` por cima). `saveStop`/`delStop`/`saveDia` chamam `renderDiaView()` pra atualizar o modal aberto. (`toggleDia` ficou sem uso, mantido inofensivo.) Commit + push.
- **2026-06-20 — PC da Empresa (cont. 7):** Estado inicial no refresh: (1) 5 grupos da sidebar começam recolhidos (`sb-group collapsed` estático + `sbGroupsOpen=false` + ícone `ti-fold-up`); (2) nada selecionado (removido `active` de `page-casamento` e `nav-cas-convidados`); (3) nova `#page-welcome` (ativa) com `.coming-soon` "Selecione uma área no menu…". Init ajustado pra limpar seleção e cair na welcome. Commit + push.
- **2026-06-20 — PC da Empresa (cont. 6):** Roteiro: (1) lista em **2 colunas** (`.roteiro-list` virou grid 1fr 1fr, com fallback 1 coluna em ≤760px); (2) bug corrigido — `roteiroDiaRico` tinha `dia-chevron open`/`dia-body open` fixos → dias nasciam expandidos no refresh. Removido (agora começam recolhidos). Commit + push.
- **2026-06-20 — PC da Empresa (cont. 5):** Cards do Roteiro mais compactos (`.dia-header` padding .85→.5rem, `.dia-header-rico` 1rem→.55rem, `.dia-num` 28→24px, `.dia-num-rico` 34→26px). Commit + push.
- **2026-06-20 — PC da Empresa (cont. 4):** Itens menores no `index.html`: botão "Exportar HTML" no menu de config estava só com ícone → adicionei o texto e alinhei ao estilo do menu. Commit + push.
- **2026-06-20 — PC da Empresa (cont. 3):** Mesclei os dias 2–5 detalhados do roteiro no SEED do `index.html` (substituindo os dias simples). O `roteiroRepair` (via `cloudApply`) re-injeta por cima do roteiro simples do Supabase → roteiro detalhado aparece **sem mexer no Supabase**. Removi o `roteiro-detalhado-dias-1-5.js`. Commit + push. **No Mac: `git pull` antes de editar.**
- **2026-06-20 — PC da Empresa (cont. 2):** Bug na versão do Mac: o `#config-menu` (menu da engrenagem) estava com `style="display: block"` fixo no HTML → abria sozinho a cada refresh. Troquei para `display: none`. Commit + push. **No Mac: `git pull` antes de editar o index.html.**
- **2026-06-20 — PC da Empresa (cont.):** Editei o `index.html` (versão do Mac) **a partir do PC**: reapliquei o filtro de erros de extensão no handler global de erro (`window.addEventListener('error'…` ignora `-extension://` e "Script error."). Isso tinha se perdido no alinhamento. Commit + push feitos → Pages atualiza e o banner "ERRO DETECTADO" (causado pela extensão Dashlane) some. **No Mac: dar `git pull` antes de editar o index.html** pra pegar essa mudança.
- **2026-06-20 — PC da Empresa:** Alinhei o PC ao Mac (git init + reset para `origin/main`). Criei este CLAUDE.md e o `roteiro-detalhado-dias-1-5.js`. Backup da versão paralela salvo localmente no PC. **Próximo passo (no Mac):** mesclar os dias 1–5 detalhados no `index.html` e dar push.
